---
description: Get the critique's verdict on a message, a leads file, or a site draft
argument-hint: <file path, site slug, or paste the message>
---
Use the **critique** subagent to review: $ARGUMENTS

- If it's a site (a path under `sites/`, or a slug where `sites/<slug>/index.html` exists), use Mode 2, including the screenshot check.
- If it's a leads file or message text I've pasted, use Mode 1.

Show me the critique's review as it is. Don't change any files unless I ask.
